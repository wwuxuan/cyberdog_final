# 备选方案

本目录内容不由正式 `main.py` 调用。

- `stage4/left_detour.py`：第四赛段左绕入口。正式联合流程也可直接使用 `main.py --stage4-detour left`。
- `stage6/`：第六赛段历次低姿倒推、中段推球、正向推球和原始 YOLO 方案。

这些文件仅用于单独测试或回溯，不应替换正式 `stage6.py`，除非完成单独真机验证。
